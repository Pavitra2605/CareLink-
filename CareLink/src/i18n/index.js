// Export constants (translations, languages, etc)
export { translations, SUPPORTED_LANGUAGES, DEFAULT_LANGUAGE, getTranslation, interpolate } from './constants';

// Export provider and hook
export { LanguageProvider, useLanguage } from './LanguageContext';

